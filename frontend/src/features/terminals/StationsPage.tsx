/**
 * Pantalla de estaciones.
 *
 * Placeholder del commit semilla: existe para que la ruta tenga a dónde apuntar y para que
 * el carril del frontend (ECO-19) empiece sobre algo montado, no sobre una carpeta vacía.
 * El listado, el alta y la búsqueda se escriben ahí.
 */
export default function StationsPage() {
  return (
    <section className="flex flex-col gap-2 p-8">
      <h1 className="text-primary text-2xl font-semibold">Estaciones</h1>
      <p className="text-text-muted text-sm">
        Pantalla en construcción. El contrato con el backend está en <code>types.ts</code>.
      </p>
    </section>
  )
}
